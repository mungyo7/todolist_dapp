{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@3.2.3",
  "dependencies": {
    "ethereum-blockies-base64": "^1.0.2",
    "recharts": "^2.15.1",
    "viem": "^2.23.5",
    "wagmi": "^2.14.12"
  },
  "scripts": {
    "lint-staged": "lint-staged",
    "next:lint": "yarn workspace @scaffold-eth/nextjs lint",
    "next:check-types": "yarn workspace @scaffold-eth/nextjs check-types",
    "hardhat:lint-staged": "yarn workspace @scaffold-eth/hardhat lint-staged"
  },
  "lint-staged": {
    "**/*.{js,jsx,ts,tsx}": [
      "prettier --write",
      "eslint --fix"
    ]
  },
  "devDependencies": {
    "lint-staged": "^15.4.3"
  }
}
